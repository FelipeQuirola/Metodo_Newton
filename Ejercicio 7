#Newton 
p1 = newton(func=lambda x: x**(1/3),
            x0=1,
            fprime=lambda x: 1 / (3*x**(2/3)))
print(p1)

#Secante
p1 = newton(func=lambda x: x**(1/3) + 1,
            x0=1)
print(p1)
